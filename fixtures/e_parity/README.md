# E-parity fixture status

The original E-parity golden values were derived from private v1 campaign outputs and are not included in this public source export. The library code retains explicit runtime inputs for recomputation where a user has authorized source data and model weights. This directory does not claim to reproduce the private campaign results.
