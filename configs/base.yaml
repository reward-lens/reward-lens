# Base configuration for v2 experiments.
# Override fields per-experiment under configs/experiments/.

n_pairs_per_dim: 150
batch_size: 32
max_length: 2048
seed: 0
n_resamples: 10000
ci: 0.95
skip_models_on_error: true
resume: true
progress: true
