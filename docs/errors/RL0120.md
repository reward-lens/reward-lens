# RL0120

the reach panel was given no task set

## What it means

the reach panel runs the grader for the subject the message names against a task, and no task set was supplied

## What this means for the run

reward-lens exits 4: usage, configuration or input contract. Nothing was measured and no record was written. Many shells use 2 for a usage mistake; here 2 never means that.

## What usually causes it, and what resolves each

- supply a task set: run: reward-lens audit . --tasks tasks.jsonl
- to read what the panel would have measured before you supply one: run: reward-lens explain RL0120
- if reach is not what this run should measure, leave the panel out rather than pointing it at an invented task, and the record carries its absence

## Offline

`reward-lens explain RL0120` prints this with the network off.
