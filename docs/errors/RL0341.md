# RL0341

the outcome check collected no tests

## What it means

the outcome check at the path the message names collected 0 tests, so it cannot qualify a correctness claim

## What this means for the run

reward-lens exits 4: usage, configuration or input contract. Nothing was measured and no record was written. Many shells use 2 for a usage mistake; here 2 never means that.

## What usually causes it, and what resolves each

- check the path, or run: reward-lens doctor --outcome <the outcome directory>
- if the suite collects from somewhere else, run it yourself first and pass the directory that worked: run: reward-lens audit . --outcome <that directory>
- an empty directory and a directory of helpers with no test in it look the same to a collector: check that the file names match the collector's rule

## Offline

`reward-lens explain RL0341` prints this with the network off.
