# RL0213

a reward function returned None and the trainer would make it NaN

## What it means

a reward function returned None for a row, and a trainer turns that into NaN for that row

## What this means for the run

Nothing stops. RL0213 is a finding the record carries, and a decision resting on the section it lands in stays unresolved until the finding is dealt with or accepted.

reward-lens exits 2: the decision asked for is unresolved, because the evidence is missing or inconclusive. Rejected and unresolved never collapse into each other.

## What usually causes it, and what resolves each

- return a number for every row, scoring 0.0 where the check does not apply, then measure again: run: reward-lens audit .
- None cannot mean no opinion here: the NaN spreads through the row's reward and that function stops counting, without anything being logged
- to see what the record holds, run: reward-lens open <the record path>, which carries the row that came back None and the value it became

## Offline

`reward-lens explain RL0213` prints this with the network off.
