# RL0701

the feature asked for is not in this build

## What it means

the capability the message names is not installed in this environment, so the measurement that rests on it cannot be made

## What this means for the run

reward-lens exits 5: a capability, service or extra the run needed is unavailable, so the measurement that rests on it cannot be made.

## What usually causes it, and what resolves each

- install the extra that carries it: run: pip install 'reward-lens[<the extra>]'
- to see every capability, what it unlocks and what it costs: run: reward-lens doctor
- the rest of the run still has an answer: what could not be measured is recorded as an absence rather than left out

## Offline

`reward-lens explain RL0701` prints this with the network off.
