# RL0215

a reward function returned a boolean and float() accepted it

## What it means

a reward function returned a bool for a row, and float() takes it, so 1.0 and 0.0 are recorded as though they were measured

## What this means for the run

Nothing stops. RL0215 is a finding the record carries, and a decision resting on the section it lands in stays unresolved until the finding is dealt with or accepted.

reward-lens exits 2: the decision asked for is unresolved, because the evidence is missing or inconclusive. Rejected and unresolved never collapse into each other.

## What usually causes it, and what resolves each

- return a float that says how much of the check was met, then measure again: run: reward-lens audit .
- if the check really is pass or fail, say so where the reward is declared, so a reader can tell two values apart from a scale that collapsed to two
- to see what the record holds, run: reward-lens open <the record path>, which carries what was returned and the float it became

## Offline

`reward-lens explain RL0215` prints this with the network off.
