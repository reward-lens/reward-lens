# RL0202

the answer is reachable from inside the graded process

## What it means

the reference answer for the task the finding names is readable from inside the graded process, so a response can copy it instead of earning the score

## What this means for the run

Nothing stops. RL0202 is a finding the record carries, and a decision resting on the section it lands in stays unresolved until the finding is dealt with or accepted.

reward-lens exits 2: the decision asked for is unresolved, because the evidence is missing or inconclusive. Rejected and unresolved never collapse into each other.

## What usually causes it, and what resolves each

- keep the reference outside the graded working directory and hand it to the grader in the task, then measure again: run: reward-lens audit .
- if the reference has to be on disk, put it behind the sandbox boundary rather than inside it
- to see what the record holds, run: reward-lens open <the record path>, which names the path that carried it

## Offline

`reward-lens explain RL0202` prints this with the network off.
