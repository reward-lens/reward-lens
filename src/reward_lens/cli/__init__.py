"""The command surface (D-19 to D-33): click 8.5, lazy verb loading through a module:function registry, rich imported only inside the human formatter, results to stdout and everything else to stderr. Parses and formats; computes nothing. Skeleton owned by P-CLI; later packets own their own verb modules under cli/verbs/.
"""
